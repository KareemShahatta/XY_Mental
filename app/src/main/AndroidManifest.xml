<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/xy_icon_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/xy_icon_new_round"
        android:supportsRtl="true"
        android:theme="@style/XY_Base"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.DeckActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.SkillViewControlActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.SavedDeckActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activities.SkillViewPlainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>